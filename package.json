{
  "name": "intent-clarifier",
  "version": "0.1.1",
  "private": true,
  "description": "Validation tooling for the Intent Clarifier agent skill.",
  "license": "MIT",
  "type": "module",
  "scripts": {
    "validate": "node scripts/validate.mjs",
    "test:unit": "node --test tests/*.test.mjs",
    "test": "npm run test:unit && npm run validate"
  },
  "devDependencies": {
    "js-yaml": "^5.2.1"
  }
}
